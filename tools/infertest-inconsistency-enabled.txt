# InferTest — enabled inconsistency/violation-style constructs
#
# One construct name per line, resolved against inconsistency-test-cases/
# (not test-cases/ -- see that directory's README for why these live
# separately). A line is ENABLED unless it starts with '#'.
# run_all_tests.py, prepare_test_ontology.py and validate_entailments.py
# all read this same file by default for their inconsistency-construct
# handling, so this is the one place to control which of them run.
#
# Each of these expects reasoning over premises.ttl ALONE (no negation, no
# merging with anything else) to make the ontology INCONSISTENT. A
# conformant OWL 2 DL reasoner must reject every one of these.

asymmetric-property
complement-of
different-individuals
disjoint-classes
disjoint-properties
disjoint-union
irreflexive-property
negative-property-assertion
has-key
